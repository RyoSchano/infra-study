# server_check.sh

## 1. 目的・背景
- サーバーの状態確認

## 2. 使い方・仕様（インタフェース）
```
実行例: server_check.sh
*引数・入力
├ 
├ 
└ 

*保存・出力
├ 場所: 標準出力
└ 形式: 
```
## 3. 処理フロー（メインロジック）
【大枠の処理の流れ】
1. 現在のユーザー表示    whoami
2. ホスト名表示         hostname
3. IPアドレス表示       hostname -I | awk '{print $1}'
4. SSHステータス表示    systemctl is-active ssh
5. ポート22状態表示     ss -tln | grep ':22 '

## 4. 内部設計（使用する値）
```
*主要変数・定数
├ 
├ 
└

```
## 5. エラー想定・例外処理
【発生しそうなエラーとその時の挙動】

## 今後の拡張予定